<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main Content -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Welcome to TaskSync!"
            android:textSize="24sp"
            android:textStyle="bold" />

    </RelativeLayout>

    <!-- FAB for Weekly (Bottommost in XML = Topmost in Z-order) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabWeekly"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="230dp"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="Weekly Planner"
        app:backgroundTint="#9C27B0"
        app:fabSize="mini"
        android:elevation="6dp" />

    <!-- FAB for To-Do -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabTodo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="160dp"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="To-Do List"
        app:backgroundTint="#FF9800"
        app:fabSize="mini"
        android:elevation="6dp" />

    <!-- FAB for Note -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabNote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="90dp"
        android:src="@drawable/ic_launcher_foreground"
        android:contentDescription="Add Note"
        app:backgroundTint="#2196F3"
        app:fabSize="mini"
        android:elevation="6dp" />

    <!-- Main FAB (Last = Top Z-order) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/common_google_signin_btn_icon_dark"
        android:contentDescription="Main Menu"
        app:backgroundTint="#4CAF50"
        android:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>