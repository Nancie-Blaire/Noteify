<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    card_view:cardCornerRadius="12dp"
    card_view:strokeColor="#f6d5b6"
    card_view:cardBackgroundColor="#f6d5b6">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:background="@color/cardview_shadow_end_color">

        <!-- ✅ NEW: Checkbox for multi-select (hidden by default, left side) -->
        <CheckBox
            android:id="@+id/selectCheckBox"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:visibility="gone"
            android:clickable="false"
            android:focusable="false"/>

        <!-- Three Dots Menu (Far Right) -->
        <ImageView
            android:id="@+id/noteMenuButton"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/three_dots"
            android:contentDescription="More options"
            android:padding="2dp" />

        <!-- Star Icon (Left of three dots) -->
        <ImageView
            android:id="@+id/starIcon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_toStartOf="@id/noteMenuButton"
            android:layout_centerVertical="true"
            android:clickable="true"
            android:focusable="true"
            android:layout_marginEnd="4dp"
            android:src="@drawable/ic_star_outline"
            android:contentDescription="Star"
            android:padding="2dp"/>

        <!-- ✅ Lock Icon (LEFT SIDE - after checkbox, before title) -->
        <ImageView
            android:id="@+id/lockIcon"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_toEndOf="@id/selectCheckBox"
            android:src="@drawable/ic_privacy_lock"
            android:contentDescription="Locked"
            android:visibility="gone"
            android:padding="2dp"
            android:layout_centerVertical="true"
            android:layout_marginStart="4dp"/>

        <!-- ✅ Content Layout (adjusted to be right of lock icon and checkbox) -->
        <LinearLayout
            android:id="@+id/noteLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/lockIcon"
            android:layout_toStartOf="@id/starIcon"
            android:layout_centerVertical="true"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/noteTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Note Title"
                android:fontFamily="@font/syne"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:maxLines="1"
                android:ellipsize="end"/>

            <TextView
                android:id="@+id/noteContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Note content preview"
                android:fontFamily="@font/syne"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#666666"
                android:layout_marginTop="3dp"
                android:maxLines="1"
                android:ellipsize="end"/>
        </LinearLayout>

    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>