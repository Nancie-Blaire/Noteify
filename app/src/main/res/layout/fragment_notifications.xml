<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f4e8df">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Notifications"
            android:fontFamily="@font/syne"
            android:textSize="25sp"
            android:textColor="#000000"
            android:layout_marginStart="5dp"
            android:textStyle="bold"
            android:layout_marginBottom="10dp"/>

        <!-- UPCOMING SECTION -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Upcoming"
            android:fontFamily="@font/syne"
            android:layout_marginStart="5dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:letterSpacing="0.05"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="8dp"/>

        <!-- Upcoming Empty State -->
        <TextView
            android:id="@+id/upcomingEmptyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No upcoming tasks"
            android:textSize="14sp"
            android:textColor="#999999"
            android:padding="16dp"
            android:gravity="center"
            android:visibility="gone"/>

        <!-- Upcoming RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/upcomingRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:nestedScrollingEnabled="false"/>

        <!-- OVERDUE SECTION -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Overdue"
            android:fontFamily="@font/syne"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginStart="5dp"
            android:letterSpacing="0.05"
            android:layout_marginBottom="8dp"/>

        <!-- Overdue Empty State -->
        <TextView
            android:id="@+id/overdueEmptyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No overdue tasks"
            android:textSize="14sp"
            android:textColor="#999999"
            android:padding="16dp"
            android:gravity="center"
            android:visibility="gone"/>

        <!-- ✅ OVERDUE LINEAR LAYOUT (replaces RecyclerView) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/overdueRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"/>

    </LinearLayout>

</ScrollView>